/**
 * ID for Currently logged in user.
 */
export const userIDKey = Symbol();

/**
 * ID for currently selected server.
 */
export const serverIDKey = Symbol();

/**
 * Whether the `lowDataMode` settings is set.
 */
export const lowDataModeKey = Symbol();
